<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:JAMA"
             x:Class="JAMA.MainPage">
    <StackLayout>
        <StackLayout>
            <Label Text=""
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
            <Label Text=""
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
            <Label Text=""
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
            <Label Text="About Me App - Jonathan Adamson"
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
        </StackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
           
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <Button Grid.Column="0" Grid.Row="0" Text="About Me"></Button>
            <Button Grid.Column="1" Grid.Row="1" Text="My Skills"></Button>
            <Button Grid.Column="0" Grid.Row="1" Text="Contact Me"></Button>
        </Grid>
    </StackLayout>
</ContentPage>
